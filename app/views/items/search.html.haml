%p 検索
= form_tag(search_path,:method => 'get') do
  = text_field_tag :search
  = submit_tag 'Search', :name => nil
%ul
  - @search_items.each do |items|
    %li
      = link_to post.search, name
      = post.created_at
      = post.status
    = link_to "編集",edit_post_path(post)
    = link_to "削除",post, method: :delete
